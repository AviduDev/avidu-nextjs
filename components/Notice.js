import styles from './Notice.module.css';

export default function Notice() {
    return (
        <section className={styles.section}>
            This website is under development, so it does not represent the final product
        </section>
    )
}